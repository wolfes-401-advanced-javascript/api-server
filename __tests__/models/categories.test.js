'use strict';

// const categories = require('../../lib/models/categories/categories-collection.js');

// const categoriesSchema = require('../../lib/models/categories/categories-schema.js')

// const supergoose = require('cf-supergoose');



// constructor tests
  // test that new constructor instantiates categories appropriately

// describe('Tests that a new constructor instantiates correctly', () => {
//   it('should instantiate a new category', () => {
//     let newCat = new categories(categoriesSchema.categoriesSchema);
//     expect(newCat.schema).toEqual(categoriesSchema.categoriesSchema);

//   })
// })

// read method tests  
  // test that if I call read with invalid id returns undefined
  // test that if I call read with valid id returns the record

// describe('Tests to verify that the read method throws an exception if invalid id is passed', () => {
//   it('should return undefined', () => {
//     let newCat = new categories(categoriesSchema.categoriesSchema);
//     expect(newCat.read('_1234')).toEqual(undefined);
//   })
// })

//create method tests
  //test that if I call create it creates a new record
  //test for failure

//update method tests
  //test that if I call update with an invalid id returns undefined
  //test that if I call update with valid id updates the record

//delete method tests
  //test that if I call delete with an invalid id returns undefined
  //test that if I call delete with valid id deletes the record
