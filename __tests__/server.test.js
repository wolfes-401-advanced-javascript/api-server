'use strict';
const server = require('../lib/server.js')
//if we start the server it starts appropriately

describe('Test to make sure the server starts correctly', () => {
  it('should start the server', () => {
    
  })
})